apiVersion: v1
kind: Service
metadata:
  name: portainer
  namespace: portainer
spec:
  type: LoadBalancer
  selector:
    app: portainer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9000  # Portainer's default port
  LoadBalancerIP: 172.16.10.205
  