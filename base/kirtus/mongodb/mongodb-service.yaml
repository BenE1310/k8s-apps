---
# MongoDB Service (ClusterIP) - for internal access
apiVersion: v1
kind: Service
metadata:
  name: mongodb-kirtus
  namespace: kirtus
  labels:
    app: mongodb-kirtus
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb-kirtus

---
# MongoDB Service (LoadBalancer) - for external access with fixed IP
apiVersion: v1
kind: Service
metadata:
  name: mongodb-kirtus-external
  namespace: kirtus
  labels:
    app: mongodb-kirtus
spec:
  type: LoadBalancer
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb-kirtus
  loadBalancerIP: 172.16.10.201
